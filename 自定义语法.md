# 自定义语法文档

本文档详细说明了在 `pages\8自定义模板语法.py` 中实现的自定义模板语法及其解析渲染功能。该模板系统支持变量引用、条件判断和各种交互控件，可用于创建动态内容和表单。

## 核心功能概述

### 1. 变量引用系统
- 支持基础变量引用和默认值设置
- 变量值自动同步到会话状态

### 2. 条件判断结构
- 支持布尔类型条件判断（基于复选框）
- 支持表达式条件判断（基于变量值比较）
- 支持嵌套的 if-elif-else 结构

### 3. 交互式控件
- 单选按钮组（Radio）
- 下拉选择框（Select）
- 布尔判断
- 条件判断

### 4. 变量管理
- 所有变量统一存储在 `default_values` 字典中
- 支持变量值的自动保存和恢复

## 语法规范

### 1. 变量引用

```
{{ 变量名 }}
{{ 变量名|default=默认值 }}
```

- **变量名**：支持英文字母、数字、下划线和中文字符
- **默认值**：可选参数，当变量未提供值时使用的默认内容
- **默认值分隔符**：仅支持等号(`=`)格式

**示例**：
```
{{ 用户名 }}
{{ 项目状态|default=未开始 }}
```

### 2. 条件判断

#### 2.1 布尔类型条件判断

```
{% if 条件名 %}
  条件成立时显示的内容
{% else %}
  条件不成立时显示的内容
{% endif %}
```

- 渲染时会创建一个复选框，用户可以勾选表示条件成立
- 条件名会作为变量存储在 `default_values` 字典中

**示例**：
```
{% if 有信访 %}
  〔信访处理〕 存在相关信访事项。
{% else %}
  〔信访处理〕 未收到相关信访事项。
{% endif %}
```

#### 2.2 表达式条件判断

```
{% if 变量名 = "值" %}
  条件成立时显示的内容
{% elif 变量名 = "其他值" %}
  其他条件成立时显示的内容
{% else %}
  所有条件都不成立时显示的内容
{% endif %}
```

- 支持多个 `elif` 分支
- 条件值需要使用双引号包围
- 比较操作符目前只支持等号(`=`)，比较变量值与指定字符串

**示例**：
```
{% if 信访渠道 = "来信" %}
  情况1：来信了{{来信内容}}
{% elif 信访渠道 = "来访" %}
  情况2：来访了{{来访内容}}
{% else %}
  其他信访渠道
{% endif %}
```

### 3. 交互式控件

#### 3.1 单选按钮组

```
{% radio 组名 %}
选项1|选项2|选项3
{% endradio %}
```

- **组名**：控件的变量名，用于在会话状态中存储用户选择的值
- **选项**：使用竖线(`|`)分隔的选项列表
- 渲染时会创建水平排列的按钮组
- 默认选中第一个选项

**示例**：
```
{% radio 信访渠道 %}
来信|来访|网上信访|电话信访
{% endradio %}
```

#### 3.2 下拉选择框

```
{% select 列表名 %}
选项1|选项2|选项3
{% endselect %}
```

- **列表名**：控件的变量名，用于在会话状态中存储用户选择的值
- **选项**：使用竖线(`|`)分隔的选项列表
- 渲染时会创建标准的下拉选择框
- 默认选中第一个选项

**示例**：
```
{% select 信访类型 %}
征地补偿|安置方案|土地权属|违法占地
{% endselect %}
```

## 变量管理机制

1. **统一存储**：所有变量值统一存储在 `st.session_state.default_values` 字典中
2. **自动保存**：用户的选择和输入会自动保存到会话状态
3. **跨组件共享**：变量可以在不同的组件和条件分支之间共享

## 完整示例

下面是一个完整的模板示例，展示了各种语法的组合使用：

```
{% radio 信访渠道 %}
来信|来访|网上信访|电话信访
{% endradio %}

{% select 信访类型 %}
征地补偿|安置方案|土地权属|违法占地
{% endselect %}

{% if 有信访 %}
  〔信访处理〕 {{信访年月}}，{{信访县乡村}}村民{{信访人姓名}}来信（访）反映该批次拟占地块{{信访反映具体内容}}。{{受理自然资源主管部门}}进行了认真调查处理，{{处理具体措施}}。目前，信访群众反映的问题已得到妥善解决，信访人表示不再上访。
{% else %}
  〔信访处理〕 该批次用地截至目前未收到相关信访事项。
{% endif %}

# 条件表达式类型if判断
{% if 信访渠道 = "来信" %}
情况1：来信了{{来信内容}}
{% elif 信访渠道 = "来访" %}
情况2：来访了{{来访内容}}
{% else %}
其他信访渠道的处理方式
{% endif %}
```

## 使用说明

1. **创建模板**：按照上述语法编写模板内容
2. **解析渲染**：将模板传递给 `ast()` 函数解析，然后用 `render_content()` 函数渲染
3. **用户交互**：用户可以通过点击按钮、选择下拉框、填写文本等方式提供输入
4. **获取结果**：变量值会自动保存到 `st.session_state.default_values` 字典中，可用于后续处理

## 注意事项

1. 所有控制标签使用 `{%` 和 `%}` 包裹，变量引用使用 `{{` 和 `}}` 包裹
2. 条件判断标签必须正确闭合（`{% if %}` 对应 `{% endif %}`）
3. 选项列表中的分隔符必须是竖线(`|`)，不要有多余的空格
4. 表达式条件判断的值必须用双引号包围
5. 模板解析错误时会显示错误提示，请检查语法是否正确

## 高级功能

1. **嵌套条件**：支持条件判断的嵌套使用
2. **变量复用**：一个变量可以在模板中多处引用，保持值的一致性
3. **默认值继承**：如果变量在会话状态中已存在，会优先使用已有值